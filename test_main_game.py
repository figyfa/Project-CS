from main_game import BoundingBox, Player

def test_calc_distance():
    """Test that the cartesian distance function works for standard coordinates"""
    assert False


def test_draw():
    assert False


def test_scan_for_player():
    assert False


class TestBoundingBox:
    def test_draw(self):
        assert False

    def test_scan_for_player(self):
        bounding_box = BoundingBox()
        player = Player()
        assert bouunding_box.scan_for_player(player) == True
